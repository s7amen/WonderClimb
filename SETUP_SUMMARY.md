# ✅ Подготовка за Локално Стартиране - Завършена

## Какво е Подготвено

### ✅ Създадени Файлове

1. **`backend/.env`** - Конфигурационен файл за backend
   - MongoDB URI: `mongodb://localhost:27017/wonderclimb`
   - JWT Secret: Автоматично генериран
   - Всички необходими environment variables

2. **`setup-local-dev.ps1`** - Автоматизиран setup скрипт
   - Проверява Node.js
   - Проверява/стартира MongoDB
   - Инсталира зависимости
   - Създава конфигурационни файлове

3. **`LOCAL_SETUP.md`** - Подробно ръководство за setup
   - Стъпка по стъпка инструкции
   - Решения на често срещани проблеми
   - Полезни команди

4. **`START_HERE.md`** - Бърз старт ръководство
   - Кратко резюме на стъпките
   - Бързи команди за стартиране

---

## Следващи Стъпки

### 1. Инсталирай Node.js (ако все още не е инсталиран)

**Проверка:**
```powershell
node --version
npm --version
```

**Ако не е инсталиран:**
- Изтегли от: https://nodejs.org/
- Инсталирай версия 20.x или по-висока
- Рестартирай терминала след инсталация

### 2. Инсталирай MongoDB (избери един вариант)

**Вариант A: Docker (Препоръчително)**
- Изтегли Docker Desktop: https://www.docker.com/products/docker-desktop/
- Стартирай Docker Desktop
- Изпълни: `docker-compose up -d mongodb`

**Вариант B: Локална Инсталация**
- Изтегли MongoDB Community: https://www.mongodb.com/try/download/community
- Инсталирай с default настройки
- Стартирай: `Start-Service MongoDB`

### 3. Инсталирай Зависимости

**Backend:**
```powershell
cd backend
npm install
```

**Frontend:**
```powershell
cd frontend
npm install
```

### 4. Стартирай Проекта

**Терминал 1 - MongoDB:**
```powershell
docker-compose up -d mongodb
# или
Start-Service MongoDB
```

**Терминал 2 - Backend:**
```powershell
cd backend
npm run dev
```

**Терминал 3 - Frontend:**
```powershell
cd frontend
npm run dev
```

### 5. Отвори в Браузър

- Frontend: http://localhost:5173
- Backend API: http://localhost:3000/api/v1
- Health Check: http://localhost:3000/health

---

## Алтернатива: Използвай Автоматизирания Скрипт

Ако искаш да използваш автоматизирания setup скрипт:

```powershell
# Разреши изпълнение на скриптове (еднократно)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Стартирай скрипта
.\setup-local-dev.ps1
```

**Забележка:** Скриптът ще се опита да инсталира зависимостите автоматично, но все пак трябва да имаш Node.js инсталиран преди това.

---

## Проверка на Статуса

### Проверка на Конфигурацията

```powershell
# Проверка на .env файл
Test-Path backend\.env
# Трябва да върне: True

# Проверка на MongoDB
docker ps | Select-String mongodb
# или
Get-Service MongoDB
```

### Проверка на Зависимостите

```powershell
# Backend
Test-Path backend\node_modules
# Трябва да върне: True (след npm install)

# Frontend
Test-Path frontend\node_modules
# Трябва да върне: True (след npm install)
```

---

## Документация

- **`START_HERE.md`** - Бърз старт
- **`LOCAL_SETUP.md`** - Подробно ръководство
- **`backend/README.md`** - Backend документация
- **`frontend/README.md`** - Frontend документация

---

## Нуждаеш се от Помощ?

Ако срещнеш проблеми:

1. Провери `LOCAL_SETUP.md` за решения на често срещани проблеми
2. Увери се, че имаш правилните версии:
   - Node.js 20.x или по-висока
   - MongoDB 7.0 или по-висока
3. Провери дали MongoDB е стартиран
4. Провери дали портовете 3000 и 5173 са свободни

---

**Всичко е готово за стартиране! 🚀**

